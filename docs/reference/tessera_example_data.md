# Synthetic TESSERA Example Data Files

The following files are stored in `inst/extdata` and are used for
runnable examples and unit tests within the TESSERA package.

## Data Generation

All datasets below were generated using the script located at:
`system.file("examples", "gen_data_and_run.R", package = "TESSERA")`
